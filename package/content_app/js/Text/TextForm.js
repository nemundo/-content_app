import ContentForm from "../../content/ContentForm.js";import TextBox from "../../framework/Form/TextBox.js";import Debug from "../../core/Debug/Debug.js";import BootstrapTextBox from "../../framework/Bootstrap/Form/BootstrapTextBox.js";export class TextContentForm extends ContentForm {    _textbox;    constructor(parentContainer) {        super(parentContainer);        this.serviceName = "text-post";    }    render() {        this._textbox = new BootstrapTextBox(this);        this._textbox.validation = true;        this._textbox.name = "text";        this._textbox.label = "Text";        this.renderSubmitButton();    }    onEditForm(dataRow) {        //(new Debug()).write("onEditRow");        //super.onEditForm(dataRow);        this._textbox.value = dataRow.text;        //this._textbox.value = dataRow.subject;    }    resetForm() {        this._textbox.value = "";        //this._textbox.setFocus();    }    validateForm() {        let value = true;        if (this._textbox.value === "") {            value = false;            this._textbox.label = "ERROR";        }        return value;    }}