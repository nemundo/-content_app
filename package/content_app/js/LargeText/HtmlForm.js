import ContentForm from "../../content/ContentForm.js";import TextBox from "../../framework/Form/TextBox.js";import LargeTextBox from "../../framework/Form/LargeTextBox.js";import BootstrapLargeTextBox from "../../framework/Bootstrap/Form/BootstrapLargeTextBox.js";export class HtmlForm extends ContentForm {    _textbox;    constructor(parentContainer) {        super(parentContainer);        this.serviceName="html-post";    }    render() {        this._textbox = new BootstrapLargeTextBox(this);        this._textbox.validation = true;        this._textbox.name = "text";        this._textbox.label = "HTML";        this.renderSubmitButton();    }    onEditForm(dataRow) {        this._textbox.value = dataRow.text;    }    resetForm() {        this._textbox.value = "";        //this._textbox.setFocus();    }    validateForm() {        let value = true;        if (this._textbox.value === "") {            value = false;            this._textbox.label = "ERROR";        }        return value;    }}